//find both number are same or not.
#include<stdio.h>
main()
{
	int  a,b;
	printf("Enter the frist number:");
	scanf("%d",&a);
	printf("Enter the Second number:");
	scanf("%d",&b);
	
	if(a==b)
        printf("Both number are same");
    else
        printf("Both number are different");

}
